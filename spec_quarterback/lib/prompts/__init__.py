"""Prompt templates for the spec_quarterback flows.

Split by flow:

* :mod:`.generation` — the lean pm-engine generator (PRD -> SpecList).
* :mod:`.refinement` — the Hermes iteration loop (critique -> improve -> score).
"""
